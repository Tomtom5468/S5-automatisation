name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'  # Mettre à jour vers PHP 8.1
          coverage: xdebug  # Ajout de xdebug pour la couverture de code

      - name: Install dependencies
        run: |
          cd PrivateBin-main  # Se déplacer vers le bon répertoire
          composer install  # Installer les dépendances

      - name: Run PHPUnit with coverage
        run: |
          cd PrivateBin-main
          ./vendor/bin/phpunit --coverage-html build/coverage-html
        env:
          XDEBUG_MODE: coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v3
        with:
          name: coverage-html
          path: PrivateBin-main/build/coverage-html
