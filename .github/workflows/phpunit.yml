name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
  - name: PHPUnit tests
      uses: php-actions/phpunit@v3
      with:
        memory_limit: "256M"
